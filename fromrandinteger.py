#   Under the random function, we can get back a random integer i.e. randint module which will give us the random number between the parameters which could be 1 or 10 and both the end parameters of the function are included. 

from random import randint

number = randint(1, 10)    # We are currently using the randint module of the random function with the parameter 1 and 10.

print(number)
