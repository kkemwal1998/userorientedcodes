while True :     # We use this while using making a user defined loop function.
    X = int(input("What's X ? "))        
    if X > 0 :
        break          # since it has become infinte function, we use break in order to end it.


for R in range(X) :
    print("meaw")


