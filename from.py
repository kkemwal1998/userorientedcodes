from random import choice            # from helps us in getting acces to a particular module of the function. It only makes our code further optimum.

coin = choice(["heads", "tails"])       # There is no need to call random because we have already called the random function.

print(coin)           